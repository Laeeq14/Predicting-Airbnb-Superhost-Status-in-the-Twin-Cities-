# ─────────────────────────────────────────────────────────────
# Superhost Predictor — Dependencies
# Version ranges chosen to work with Python 3.11+ (incl. 3.14)
# and to have pre-built wheels available on Windows.
# ─────────────────────────────────────────────────────────────

# Web layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Data & ML
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.0
shap>=0.44.0

# Agent (LLM + env)
openai>=1.30.0
python-dotenv>=1.0.0

# ─────────────────────────────────────────────────────────────
# Drift Monitoring (Evidently AI)
# ─────────────────────────────────────────────────────────────
evidently>=0.4.30

# ─────────────────────────────────────────────────────────────
# CI / Dev tooling
# ─────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0          # ASGI test client for FastAPI
pytest-cov>=5.0.0
ruff>=0.4.0

